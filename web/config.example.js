// Google Cloud Console Configuration
// Replace these values with your actual credentials from Google Cloud Console
// https://console.cloud.google.com/

// 1. Create a new project in Google Cloud Console
// 2. Enable Google Drive API
// 3. Create OAuth 2.0 credentials (Web application)
// 4. Add authorized JavaScript origins and redirect URIs

const CLIENT_ID = 'YOUR_CLIENT_ID.apps.googleusercontent.com';
const API_KEY = 'YOUR_API_KEY';

// Copy this file to config.js and update the values above
// Make sure to add config.js to .gitignore to keep your credentials secure
